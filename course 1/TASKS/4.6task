hrs = input("Enter Hours:")
h = float(hrs)
rate = input("Enter Rate:")
r = float(rate)
def computepay(h, r):
	if h <= 40:
		p = h*r
	elif h > 40:
		p = r*(h - 40)*1.5 + 40*r
		return(p)

computepay(h, r)
p = computepay(h, r)
print("Pay", p)
